[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg", "python3", "curl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp",
  "chmod +x /usr/local/bin/yt-dlp"
]

[start]
cmd = "node index.js"
