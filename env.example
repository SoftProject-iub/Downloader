# ── Railway Variables ──────────────────────────────
# Set ONLY these in Railway dashboard → Variables

PHONE_NUMBER=923451036359     # Your WhatsApp number (no + sign)
CLIENT_ID=sheezzi-bot
SESSION_PATH=/data/wwebjs_auth  # Requires a Railway Volume mounted at /data
TMP_DIR=/tmp/sheezzi

# ⚠️  DO NOT SET these — they are auto-resolved at runtime:
# PUPPETEER_EXECUTABLE_PATH  ← DELETE this from Railway if you set it before
# YTDLP_PATH                 ← auto-detected
# FFMPEG_PATH                ← auto-detected
